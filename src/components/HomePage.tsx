export const HomePage = () => (
  <section>
    <h1>Home</h1>
    <p>
      Eager route. Keep this tab open, rebuild the app, then navigate to a lazy
      route to trigger a stale chunk load.
    </p>
  </section>
);
